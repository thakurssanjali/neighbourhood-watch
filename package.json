{
  "name": "neighbourhood-watch",
  "version": "1.0.0",
  "description": "A community-based incident reporting and neighborhood watch application",
  "main": "server/index.js",
  "scripts": {
    "dev": "npm run dev:all",
    "dev:all": "npm-run-all --parallel dev:server dev:client",
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev",
    "build": "npm-run-all build:client",
    "build:client": "cd client && npm run build",
    "build:server": "echo 'Server is not built, it runs directly with Node.js'",
    "start": "node server/index.js",
    "install-deps": "npm install && cd server && npm install && cd ../client && npm install && cd .."
  },
  "keywords": [
    "neighborhood",
    "watch",
    "incident",
    "reporting",
    "community"
  ],
  "author": "Sanjali Thakur",
  "license": "MIT",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
